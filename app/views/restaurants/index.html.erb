<h1>Fav restaurants</h1>

<% @restaurants.each do |r| %>
  <p><%= link_to r.name, restaurant_path(r) %></p>
  <p><%= link_to "Edit", edit_restaurant_path(r) %> | <%= link_to "Delete", restaurant_path(r), method: :delete %></p>
<% end %>

<p><%= link_to "Add restaurant", new_restaurant_path %></p>
